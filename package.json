{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "yarn workspaces run clean",
    "build": "yarn workspaces run build",
    "test": "mocha"
  },
  "devDependencies": {
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.7.23",
    "esbuild": "^0.15.9",
    "esbuild-register": "^3.3.3",
    "mocha": "^10.0.0",
    "typescript": "^4.8.3"
  }
}
